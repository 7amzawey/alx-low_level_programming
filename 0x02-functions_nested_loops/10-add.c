#include "main.h"
/**
 * add - write a function that adds two integers and returns the result
 * @i: first integer to add
 * @j: second integer to ad
 * Return: the sum of i and j
 */
int add(int i, int j)
{
	int s;

	s = i + j;
	return (s);
}
