#include "main.h"
#include <stdlib.h>
/**
 * *create_array - creats an array of chars and 
 * intializes it with a specific char.
 * @size: is the size of the array
 * @c: is the chars that will be allocated
 * Return: NULL if size = 0. the pointer to the array
 * or NULL if it fails
 */

char *create_array(unsigned int size, char c)
{
	ar = malloc(size * sizeof(char));
	while( size != 0)
	{
		_putchar(ar[i]);
		i++;
		size--;
	}
	if (size == 0)
	{
		Return;
	}
	return (0);
}
		
